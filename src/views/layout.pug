doctype html
html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel="stylesheet" href=toSiteUrl("/styles.css"))
    title= pageTitle || 'Split Payment Tracker'

  body
    include partials/header.pug

    main
      block content